{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Tipos do Vite: declara import de .css/.svg e o `import.meta.env`. */
    "types": ["vite/client"],

    /* Path alias — imports absolutos evitam "../../../" e sobrevivem a refactors */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    /* Strictness — regras que um code review de empresa cobraria */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    "noUncheckedIndexedAccess": true
  },
  "include": ["src"],
  /*
   * Testes unitários rodam no Node, não no browser: precisam de outros tipos globais.
   * Ficam em `tsconfig.test.json` para que o código da aplicação não ganhe acesso
   * acidental às APIs do Node — o que compilaria aqui e quebraria em produção.
   */
  "exclude": ["src/**/*.test.ts"]
}
